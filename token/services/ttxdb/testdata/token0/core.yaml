token:
  enabled: true
  ttxdb:
    persistence:
      type: sql
      opts:
        createSchema: true
        tablePrefix: tsdk
        driver: sqlite
        dataSource: db.sqlite?_pragma=journal_mode(WAL)&_pragma=busy_timeout(5000)