token:
  enabled: true
  tms:
    pineapple:
      network: pineapple
      channel:
      namespace:
      identitydb:
        persistence:
          type: sql
          opts:
            tablePrefix: tsdk
            driver: sqlite
            maxOpenConns: 1
            dataSource: db.sqlite?_pragma=journal_mode(WAL)&_pragma=busy_timeout(5000)
    grapes:
      network: grapes
      channel:
      namespace:
      identitydb:
        persistence:
          type: sql
          opts:
            tablePrefix: tsdk
            driver: sqlite
            maxOpenConns: 1
            dataSource: db.sqlite?_pragma=journal_mode(WAL)&_pragma=busy_timeout(5000)